from math import fabs

positive = 5
negative = -5

print(fabs(positive))
print(fabs(negative))

# fabs - это функция которую мы импортируем из модуля math. Она работает так же как и abs, но возвращает тип данных float.